# GuelRust — 구엘룰(Guel-Sword Rule) 필승 끝말잇기 AI 엔진

| 항목 | 값 |
|---|---|
| 문서 버전 | v1.0.0 |
| 상태 | 초안 (Review 필요) |
| 작성일 | 2026-09-10 |
| 작성자 | PM / Software Architect |
| 저장소 | github.com/ax1s-x1zz/GuelRust |
| 관련 문서 | AGENTS.md (개발 에이전트 지침) |

---

## 1. 프로젝트 개요 (Overview)

### 1.1 프로젝트명 및 목적

- **프로젝트명**: `GuelRust`
- **제품 정의**: 대한민국 표준어 사전 전체를 오프라인으로 사전-연산(precompute)하여, **역방향 탐색(Retrograde Analysis)**으로 모든 게임 상태의 승패(W/L)를 확정한 뒤 **인간을 100% 이기는** 필승 끝말잇기 AI 엔진.
- **핵심 명제**:
  - 완전 정보(perfect information) + 유한 상태 게임으로 모델링.
  - 모든 단어를 실시간으로 추적하여, **상대에게 방어 단어가 하나도 없는 "한방(끝) 음절"을 L0(즉시 패배)로 규정**.
  - 내가 낸 단어가 상대를 L(n)으로 보내면 그 단어/음절은 **W(n+1)**, 어떤 단어를 내도 상대가 나를 W(n)으로 이끌면 그 음절은 **L(n)**.
  - 구엘룰의 특수 조건(**한글 두음법칙**, **후공의 스왑/단어 뺏기 권한**)을 완전 반영.
  - 시작 포지션의 W/L을 정확히 판정하여, **필승 포지션이면 선공/후공을 능동적으로 선택**함으로써 "100% 승리"를 보장.

> **100% 승리 정의 (중요)**: 역방향 탐색 결과 시작 음절이 이론상 W(선공 승)이면 선공을, L이면 후공(스왑 활용)을 자동 선택하고 최선수를 두어 **절대 지지 않음**. 실질적으로 "상대가 합리적으로 대응해도 이길 수 없는 유저는 없다"는 의미의 필승 전략.

### 1.2 타겟 페르소나 및 사용성

| 페르소나 | 니즈 | 제공 가치 |
|---|---|---|
| 끝말잇기 고수 / 랭커 | 필승 수순, 한방 단어 탐색 | 이론 최선수 + 한방 사전 |
| 스트리머 / 방송인 | 실시간 방송용 인터랙티브 대국 | 턴당 < 50ms 응답의 인터랙티브 CLI |
| AI 봇 플랫폼 운영자 | 프로그래매틱 연동 | 라이브러리 API + (v2) gRPC 서버 |

**사용성 원칙**:
- 턴당 응답 **P99 < 50ms** (실시간성).
- 단일 정적 바이너리(CLI)로 즉시 실행.
- 배치(스펙트레이션) 모드로 이론 검증 가능.
- 외부 규칙(스왑 회수·조건, 중립 처리)은 **설정 파일로 주입 가능**하여 룰 변형에 유연.

---

## 2. 핵심 기능 요구사항 (Functional Requirements)

상위 레벨 → 하위 레벨로 그룹화. 각 항목은 한 줄의 명확한 문장(punchy fragment)으로 표현.

### 2.1 데이터 수집 & 파싱
- **FR-D1** 표준국어대사전/우리말샘(open dict) CSV·JSON 로더 제공.
- **FR-D2** 자소(초성/중성/종성) 분해 전처리로 단어를 canonical 인코딩으로 변환.
- **FR-D3** 중복 단어·동음이의어를 ID로 병합하고 중복 모서리를 다중 엣지로 유지.
- **FR-D4** 품사·표준어 여부·고유명사·외래어 메타 태그를 부여.
- **FR-D5** 유니코드 NFC 정규화 및 제어문자·이모지·공백 노이즈 필터.
- **FR-D6** 사전 버전/해시를 기록하여 재현 가능한 빌드를 보장.

### 2.2 그래프 구축
- **FR-G1** 단어를 `(시작소리 key_in → 끝소리 key_out)` 모서리로 변환.
- **FR-G2** 끝소리(대표음) 역방향 인덱스 — "어떤 끝소리에 어떤 단어가 도달하는가"를 O(1)로 조회.
- **FR-G3** 두음법칙 변환 테이블(ㄹ→ㄴ/ㅇ, ㄴ→ㅇ, 겹받침 대표음) 적용.
- **FR-G4** 노드/엣지를 u16 키로 압축 인코딩하여 메모리 최소화.
- **FR-G5** 프리컴파일 인덱스를 바이너리로 직렬화하고 mmap 제로-카피 로드.
- **FR-G6** 노드(음절 소리) 기준 메타데이터(레벨 테이블, 인접 목록) 포함.

### 2.3 엔진 연산 (Retrograde Analysis)
- **FR-E1** L0(한방) 집합을 정확히 산출 — 나가는 단어가 0개인 끝소리.
- **FR-E2** 역방향 고정점(Level) 전파로 모든 노드에 W(n)/L(n) 레벨 할당.
- **FR-E3** 미분류(루프) 노드를 중립(D)으로 분류하고 정책 적용.
- **FR-E4** 플레이된 단어를 반영한 **증분(incremental) 재계산**으로 실시간성 보장.
- **FR-E5** 스왑 규칙을 상태 확장에 반영하여 후공 전략을 정확히 모델링.
- **FR-E6** 시작 음절의 W/L을 판정하여 필승 사이드(선공/후공) 자동 선택.
- **FR-E7** 한방 방어 실패 시 그리디 최선수 폴백 제공.

### 2.4 인터페이스 (Interface)
- **FR-I1** 인터랙티브 CLI — 유저 입력/스왑 선언/히스토리 표시.
- **FR-I2** 배치(스펙트레이션) 모드 — 주어진 시작음에 대한 최선수·한방·레벨 덤프.
- **FR-I3** Rust 라이브러리 API — `Engine::new()`, `best_move()`, `classify(state)` 노출.
- **FR-I4** (v2) gRPC/REST 서버 및 WASM 타깃 — 범위 확장.

### 2.5 검증 & 품질
- **FR-Q1** 랜덤 플레이오프(rollout) 자가검증 — AI가 필승 포지션에서 패배 0회.
- **FR-Q2** 전체 상태 W/L/D 테이블을 스냅샷 덤프하여 회귀 검증.
- **FR-Q3** 벤치마크(턴 응답, 프리컴파일 시간, 메모리) 자동 측정.
- **FR-Q4** 사전 노이즈 제거 후 단어 커버리지(통과율) 리포트.

---

## 3. 기술 스택 및 아키텍처 (Technical Stack & Architecture)

### 3.1 Rust 선택 이유
- **단일 정적 바이너리**로 배포 — 타깃 유저가 즉시 실행.
- **제로-코스트 추상화 + 소유권 모델** — 핫 루프에서 GC 없이 결정적 성능.
- **안전한 인덱스** — `u16` 키와 bitset 연산을 타입으로 보장.
- **데이터 병렬성** — `rayon` 기반 무공유 병렬 전파로 수백만 노드 처리.
- **제로-카피 메모리 맵** — 대형 프리컴파일 인덱스를 swap 없이 상주.

### 3.2 레이어 아키텍처
```
+------------------------------------------------------------+
|  Interface   : cli (interactive/batch) · lib API · (v2) rpc |
+------------------------------------------------------------+
|  Engine      : Retrograde Core · Incremental Updater        |
|               Swap Engine · Side Selector · Policy          |
+------------------------------------------------------------+
|  Model/Graph : Node(Key) · Multi-Edge · Level Table ·       |
|                Played Bitset · Reverse Index                |
+------------------------------------------------------------+
|  Korean      : Jamo(codec) · 두음법칙 Table · 대표음(겹받침) |
+------------------------------------------------------------+
|  Data        : Dict Loader · Parser · Normalizer · Serializer|
+------------------------------------------------------------+
```

**모듈 구성 (프로젝트 루트)**:
- `crates/guel-core` — 그래프·역방향 탐색·증분·스왑 엔진 (no_std 호환 지향).
- `crates/guel-kor` — 자소 분해·두음법칙·대표음(겹받침) 규칙.
- `crates/guel-dict` — 사전 파서·정규화·직렬화.
- `crates/guel-cli` — 인터랙티브/배치 CLI 바이너리.
- `crates/guel-meta` — 설정·폴리시·로깅·메트릭.

### 3.3 추천 크레이트 (Crate) 목록
| 크레이트 | 용도 | 대안 |
|---|---|---|
| `jamo` | 한글 자모 분해/결합 | 자체 산술 모듈(권장: 0-dep) |
| `unicode-segmentation` | grapheme 경계 처리 | — |
| `rayon` | 병렬 레벨 전파 | `std::thread` + channel |
| `crossbeam` | scoped threads / epoch | — |
| `clap` | CLI 파서 | `argh`, `gumdrop` |
| `crossterm` | 실시간 키 입력/렌더 | `rustyline` |
| `postcard` | 프리컴파일 인덱스 직렬화 | `bincode` |
| `memmap2` | 인덱스 제로-카피 로드 | `libc::mmap` |
| `bitvec` | played-추적 bitset | 커스텀 u64 bitset(권장) |
| `tracing` | 구조화 로깅 | `log` + `env_logger` |
| `insta` | 스냅샷 테스트 | — |
| `nextest` | 병렬 테스트 러너 | `cargo test` |
| `codspeed` | 벤치마크(LLVM 지시어) | `criterion` |
| `flamegraph`(cargo) | 프로파일링 | `tralfamadore`, `dhat` |

> **성능 최적화 원칙**: 자모 분해·bitset·레벨 갱신 등 최핫 경로는 **외부 크레이트 대신 자체 구현**하여 0-dep·SIMD 친화적으로 유지.

### 3.4 메모리 / 속도 최적화 전략
- **노드 키 압축**: 초성 19 × 중성 21 × 종성 28 → 실제 사용 키만 `u16` 인덱스로 매핑 (가나: 0xAC00 산술).
- **레벨 테이블**: 노드당 `u8` 배열 — `L0,L1,…` 및 `W1,W2,…`를 비트/레벨 값으로.
- **played 추적**: 각 엣지 카운트를 `u32` + 64-bit bitset으로 관리, 증분 반영.
- **병렬 전파**: 레벨 갱신을 `rayon` 파티션으로 나눠 무공유 갱신 후 합병.
- **결정성**: 병렬 실행에도 동일 입력→동일 출력 (seeded, idempotent 전파).

---

## 4. 알고리즘 및 데이터 구조 상세 (Algorithm & Data Structure)

### 4.1 기호 정의
- **단어** `w`: 
  - `key_in(w)` = 시작소리 키 (두음법칙 적용 후의 발음 초성, 또는 받침 없음 시 첫 음절 전체).
  - `key_out(w)` = 끝소리 키 (마지막 음절의 대표음 종성, 또는 받침 없음 시 마지막 음절).
- **그래프** `G=(V,E)`: 노드 `v ∈ V` = 소리 키; 모서리 `e = (key_in → key_out)`.
- **상태** `s = (cur, R, turn)`: `cur` = 현재 이어야 할 소리 키, `R` = 잔여 단어 멀티셋, `turn` = 현재 차례.
- **분류**: `W(n)` = n수 내 필승, `L(n)` = n수 내 필패, `D` = 중립(루프/무승부).

### 4.2 역방향 탐색(Retrograde Analysis) 메커니즘

**점화 규칙** (Level-annotated fixed point):

```
L0 := { v ∈ V : out_deg_R(v) == 0 }          // 이 음절에서 방어 단어 0개 → 즉시 패배
W(n+1) := { v : ∃ e ∈ R(v) with out(e) ∈ L(≤n) }   // 상대를 L로 보내는 수 존재
L(n+1) := { v : ∀ e ∈ R(v), out(e) ∈ W(≤n) }        // 모든 수가 상대에게 W만 허용
```

**전파 알고리즘**:
1. `L0` 산출 → 이 끝소리를 갖는 모든 단어의 `key_out`을 컬렉션.
2. `L0` 노드를 끝으로 갖는 모서리의 **역방향 원천**(`key_in`)들을 `W1`로 마킹.
3. `W1`만으로 도달 가능한 노드(모든 나가는 모서리가 `W(≤1)` 도달)를 `L1`로 마킹.
4. 레벨을 올리며 반복 — 새 분류가 없을 때까지.
5. 수렴 후 미분류 노드 = **중립(D)**.

**정확성 및 근사 전략**:
- 단어가 1회만 사용 가능(단일 사용)하므로 이론적으로 "Edge Geography" 계열 = **PSPACE-hard** 일반 영역.
- 실용 해법: **정적 레벨**을 멀티그래프(다중 엣지 = 독립 자원 가정)로 선형 전파 후,
  - **한방(L0) 판정은 항상 정확** — 전역 역방향 인덱스로 완전 증명.
  - 승부가 결정적인 열린 포지션(잔여 단어 수 ≤ 예산 B)에 한해 **예산 한정 정밀 탐색(IDDFS + Transposition Table)**으로 정확 검증.
- 기본 예산 `B = 30` — 실전 열린 포지션의 분기 수는 충분히 작아 P99 < 50ms 유지.

```
fn retrograde(G, R):
    L0 = { v : out_deg_R(v)==0 }
    level = {}
    pending = queue(L0 with level 0)
    while let Some((v, lvl)) = pending.pop():
        for e in incoming_R(v):          // v를 key_out으로 갖는 모든 잔여 모서리
            u = e.key_in
            if u ∉ classified:
                if u has any remaining edge to an L-node → classify W(lvl+1), enqueue
                else if u has *no* remaining edge to a W(≤lvl) node → classify L(lvl+1), enqueue
    return classify remaining as D
```

### 4.3 두음법칙 및 스왑(Swap) 예외 처리 흐름

#### 4.3.1 두음법칙 (word-initial sound law)
- **끝소리 추출** `key_out(w)`: 마지막 음절의 **대표음 종성** (겹받침 단순화 규칙):
  - `ㄳ→ㄱ, ㄵ→ㄴ, ㄺ→ㄱ, ㄼ→ㅂ, ㄽ→ㄹ, ㄾ→ㄹ, ㅄ→ㅂ, ㄻ→ㅁ, ㄿ→ㅍ, ㄲ→ㄱ, ㅆ→ㅆ, ㄶ→ㄴ, ㅀ→ㄹ, ㄹㅎ/ㄴㅎ→ㅎ(연음 시)`
  - 받침 없음(개음절): `key_out(w)` = 마지막 음절 자체(초성+중성).
- **시작소리 검증** `key_in(w)`: 단어 첫 음절의 **발음 초성**(두음법칙 적용 후)을 매칭 키로 사용.
- **허용 초성 집합** — 끝소리 `c`에서 파생되는 허용 시작소리 `A(c)`:
  - `A(ㄹ) ⊇ {ㄹ, ㄴ, ㅇ}` (두음법칙: ㄹ→ㄴ/ㅇ — ㅇ은 모음 시작).
  - `A(ㄴ) ⊇ {ㄴ, ㅇ}` (特定 모음: ㄴ+ㅑ/ㅕ/ㅛ/ㅠ/ㅣ → ㅇ).
  - 그 외 종성은 자기 자신.
- **구현**: 두음법칙 변환 테이블을 설정(파일)으로 주입, 국립국어원 표준 규정을 기본값으로.
- **흐름**: 플레이 시 `key_in(w) ∈ A(cur)` 검사 → 불일치 시 **규칙 위반(사용 불가)** 판정 → 스왑 판정 단서로 활용.

#### 4.3.2 스왑(Swap / 단어 뺏기) — 후공 권한
- **개념**: 후공은 선공이 방금 낸 단어를 **뺏아 자신의 단어로 귀속**시킬 수 있는 권한.
- **상태 확장**: `s = (cur, R, turn, owner, swapAvail)` — `owner` = 마지막 단어 소유자, `swapAvail` = 잔여 스왑 횟수.
- **전이 규칙** (스왑 선언 시):
  1. 선공이 단어 `X`(끝소리 `t`)를 플레이.
  2. 후공이 "스왑" 선언 → `X`의 소유권이 후공으로 이전.
  3. 체인은 유지되며(`t` 유지), **턴이 선공에게 복귀** — 선공이 `t`부터 이어야 함.
  4. 스왑은 **`t`가 L0(한방)일 때 금지** (사실상 한방 회피 불가).
- **설정 파라미터** (룰 파라미터 파일 `rules.toml`로 주입):
  - `swap_max_per_game` (기본 1회), `swap_max_per_player`.
  - `swap_on_winning_word` 허용 여부, `swap_after_swap` 금지 여부.
- **전략 영향**: 스왑은 후공에게 "턴 회전 + 소유권 이전" 수단을 제공하므로, 역방향 탐색에서 후공 포지션의 승패가 달라짐. 엔진은 스왑 여지를 포함한 **확장 상태**에 대해 최선수를 계산하여 필승 사이드를 선택.
- **정확 규정 미확정 리스크**: 구엘소드룰 공식 세부(스왑 회수/조건)는 외부 규정에 의존 → **§8 오픈 이슈**로 격리, 파라미터 추상화로 완충.

### 4.4 데이터 구조 상세
- **인덱스**: `key_in` → `Vec<(u16 key_out, word_id)>` (인접 목록). 역방향: `key_out` → `Vec<word_id>` (한방 탐색용).
- **두음법칙**: `A(c)` 를 28×28 bitset으로 표현.
- **레벨**: 노드별 `u8` (`0=L0,1=L1,…; hi 비트로 W 구분`).
- **played 추적**: `Vec<u32>` 엣지 카운터 + `BitVec`로 단어 사용 여부.
- **프리컴파일 포맷**: 헤더(버전·사전 해시) + 노드 테이블 + 엣지 블록 → `postcard` 직렬화 → mmap.

---

## 5. 제약 조건 및 예외 처리 (Constraints & Edge Cases)

### 5.1 실시간성 (연산 시간 제한)
- 턴당 응답 **P99 < 50ms** (증분 갱신 + 국소 정밀 탐색).
- 프리컴파일(레벨 전파) **≤ 10s** (전체 사전, 병렬).
- 예산 한정 정밀 탐색 `B=30` 초과 시 정적 레벨로 폴백 (응답 상한 보장).

### 5.2 사전 데이터 노이즈 처리
- 외래어/고유명사/인명/지명/줄임말/오타에 대한 **포함/제외 정책** (기본: 표준국어대사전 표준어 우선).
- 우리말샘(오픈) 사전 품질 변동 대비 **검증 체크리스트**: NFC 정규화, 길이 ≥ 2, 표준 종성 규칙, 두음법칙 위반 단어 필터.
- 사전 버전 해시 기록으로 재현성 보장.

### 5.3 중립 음절(루프) 처리
- 수렴 후 미분류 노드 = **D(중립/무승부)**.
- 정책(설정 가능):
  - `draw_declare`: 루프 진입 시 무승부 선언.
  - `repeat_rule`: 동일 단어 3회 반복 금지 카운터로 루프 강제 종료.
  - `turn_priority`: 교착 시 선공/후공 우선 판정.
- 엔진은 D 노드 진입 시 최소 손실(최장 연장) 수순을 기본 선택.

### 5.4 기타 엣지 케이스
- 개음절 끝(받침 없음) → 다음 단어 첫 음절 = 동일 음절 규칙 처리.
- `ㅇ` 초성(모음 시작)과 `ㄹ/ㄴ` 두음법칙 중첩.
- 스왑 선언 후 턴 복귀 시 이중 스왑 금지.
- 빈 사전/단일 단어 사전 — L0 전부, 메시지 처리.
- 유니코드 정규화 미일치, 제어 문자, 개행 포함 단어.

### 5.5 메모리 및 자원
- 프리컴파일 인덱스 **64MB 이내 바운드** (u16 키 + bitset 압축).
- mmap 로드 실패 시 안전한 인메모리 폴백.

---

## 6. 수용 기준 (Acceptance Criteria)
- [ ] AC-1: 전체 사전 단어의 W/L/D 레벨 테이블 생성 및 스냅샷 덤프.
- [ ] AC-2: 랜덤 플레이오프 10,000회에서 필승 포지션 AI 패배 **0회**.
- [ ] AC-3: 턴 응답 P99 < 50ms, 프리컴파일 ≤ 10s, 인덱스 ≤ 64MB.
- [ ] AC-4: 두음법칙(ㄹ→ㄴ/ㅇ, ㄴ→ㅇ, 겹받침) 전 단위 테스트 통과.
- [ ] AC-5: 스왑 시나리오(선공→후공 소유권 이전→턴 복귀) 전이 검증.
- [ ] AC-6: 중립(루프) 정책별 행동 검증.

---

## 7. 마일스톤 (Milestone)
| 단계 | 범위 | 목표 |
|---|---|---|
| **M0** | 사전 로더 + 자모/두음법칙 코덱 | 파싱·정규화 완료 |
| **M1** | 그래프 구축 + 정적 역방향(Level) | W/L/D 테이블 산출 |
| **M2** | 증분 갱신 + 예산 한정 정밀 탐색 | 실시간(50ms) 달성 |
| **M3** | CLI(인터랙티브/배치) + rollout 검증 | 수용 기준 절반 |
| **M4** | 스왑 규칙 완성 + 필승 사이드 선택 | 100% 필승 전략 |
| **M5** | (v2) gRPC/REST 서버 + WASM | 확장 배포 |

---

## 8. 오픈 이슈 / 결정 필요 사항 (Open Decisions)
1. **스왑 규정 확정**: 구엘소드룰 공식 규정의 정확한 회수·조건·제약을 `rules.toml` 파라미터로 최종 반영 필요.
2. **중립(루프) 정책**: 무승부 선언 vs 반복 금지 vs 선후공 우선 — 경쟁 규칙에 맞는 기본값 결정.
3. **사전 소스/라이선스**: 표준국어대사전 vs 우리말샘 — 사용 라이선스와 커버리지 트레이드오프 확정.
4. **100% 승리 범위**: "필승 포지션에서의 승리"로 한정할지, 개방 시작음 모두에서 필승이 되도록 사이드 자유 선택을 허용할지 확정.

---
*문서 종료. 개발 착수 전 §8 항목 우선 협의 권장.*
