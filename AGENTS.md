# AGENTS.md — AI 개발 에이전트 지침 명세서

이 문서는 `GuelRust` 프로젝트에서 AI 에이전트(및 협업 개발자)가 반드시 따라야 할 운영 규칙입니다. 코드를 작성·수정·검토할 때 아래 규칙을 준수하십시오.

> PRD 문서: `PRD.md` — 기능·아키텍처·알고리즘의 단일 진실 공급원(SSOT). 구현 전 반드시 PRD를 먼저 읽으십시오.

---

## 1. 커밋 규칙 (Commit Policy)

### 1.1 최소 기능 단위 커밋 (핵심 원칙)
- **하나의 커밋 = 하나의 논리적 변경.** 한 커밋에 여러 무관한 작업을 섞지 않는다.
- 작업을 **가능한 한 작게 쪼개** 자주 커밋한다. (예: "자모 코덱 추가", "두음법칙 테이블 추가"는 별도 커밋.)
- 규모가 크면: **기능 추가 → 테스트 → 리팩터링** 을 각각 별도 커밋으로 분리.

### 1.2 커밋 메시지 컨벤션 (Conventional Commits)
```
<type>(<scope>): <한국어 요약>

- 본문: 무엇을/왜 바꿨는지. 필요 시 bullet.
```
- `type`: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `perf`, `build`, `ci`, `revert`.
- `scope`(선택): `core`, `kor`, `dict`, `cli`, `engine`, `swap` 등 모듈명.
- **요약은 한국어 명령형**으로 짧게 (예: `feat(kor): 자모 분해 코덱 추가`).
- 본문은 영어/한국어 모두 허용하되, 한 프로젝트 내 일관 유지.

### 1.3 커밋 전 필수 검사
- [ ] `cargo fmt` 통과 (format)
- [ ] `cargo clippy -- -D warnings` 통과
- [ ] `cargo test` 통과
- [ ] 관련 벤치/검증 통과 (있는 경우)
- [ ] 시크릿(키, 토큰) 미포함 확인

---

## 2. 브랜치 및 PR 규칙
- 기본 브랜치: `main`.
- 신규 기능은 `feat/<이슈요약>` 브랜치에서 작업.
- PR 1개 = 1개 이상의 **최소 단위 커밋**. 리뷰어가 커밋 단위로 검토 가능하도록 유지.
- 병합: `--no-ff` (또는 squash 병합 허용 — 프로젝트 방침에 따름).

---

## 3. 코드 스타일 & 품질
- **네이밍**: 모듈/구조체/함수는 PRD의 용어를 그대로 사용 (`key_in`, `key_out`, `Level`, `L0`, `W(n)`, `swapAvail` 등).
- **주석**: 불필요한 주석 금지. 필요한 곳에만 "왜"를 설명.
- **에러 처리**: `thiserror`/커스텀 에러 타입으로 명시적 처리, `unwrap`/`panic` 최소화 (핫 경로 제외).
- **핫 경로 최적화**: 자모 분해·bitset·레벨 갱신 등은 0-dep·SIMD 친화 구현 유지.
- **결정성**: 병렬 실행 결과는 항상 동일해야 함 (seeded, idempotent).

---

## 4. 테스트 요구사항
- 모든 로직 모듈(`kor`, `dict`, `engine`, `swap`)에 단위 테스트 필수.
- 엣지 케이스 필수 커버:
  - 두음법칙: `ㄹ→ㄴ/ㅇ`, `ㄴ→ㅇ`, 겹받침 대표음.
  - 개음절(받침 없음) 이음.
  - 스왑: 소유권 이전 → 턴 복귀 → 이중 스왑 금지.
  - 중립(루프) 노드, L0(한방) 노드.
- 스냅샷 테스트(`insta`)로 레벨 테이블 회귀 검증.
- 변경 시 기존 테스트가 깨지지 않도록 반드시 `cargo test` 실행.

---

## 5. 작업 플로우 (Workflow)
1. `PRD.md` 해당 섹션 확인 → 요구사항 파악.
2. 최소 단위 작업 정의 → 구현.
3. 단위 테스트 작성 → `cargo fmt && cargo clippy && cargo test`.
4. 최소 단위 커밋 → 푸시.
5. 필요 시 PR 생성.

---

## 6. 금지 사항
- 시크릿(API 키, 토큰, 인증 정보) 커밋 금지.
- `main`에 직접 force-push 금지.
- PRD와 상충하는 동작 추가 시 커밋 전 협의.
- 불필요한 파일(빌드 산출물, 로그, `.env`) 커밋 금지.

---

## 7. 참고
- 기능/아키텍처 상세: `PRD.md`
- 이 문서 변경도 최소 단위 커밋 원칙을 따름.
